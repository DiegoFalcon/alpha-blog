<h1 align = "center"> Create new category </h1>

<%= render 'shared/errors', obj: @category %>
<div class = 'row'>
    <%= form_for @category do |f| %>
    <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: "form-control", placeholder: "Enter the name", autofocus: true %>
    </div>
    <div class = "form-group">
      <%= f.submit "Create", class: "btn btn-primary" %>
    </div>
    <% end %>
</div>

[ <%= link_to "Cancel request and back to home page", articles_path %> ]
